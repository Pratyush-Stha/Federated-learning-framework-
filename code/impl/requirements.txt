# ?? Core (always required) ??????????????????????????????????????????
# torch / torchvision come pre-installed on Colab. For local CPU-only
# install use:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
torchvision>=0.17
numpy>=1.26
scipy>=1.11
pandas>=2.1
matplotlib>=3.8
tqdm>=4.66

# ?? HMM, queueing, RL building blocks ????????????????????????????????
scikit-learn>=1.4
hmmlearn>=0.3
simpy>=4.1

# Flower + Ray (required for fl.simulation.start_simulation in QRQFL_colab.ipynb and main.py)
flwr[simulation]>=1.8.0,<2

# ?? Optional: RL libraries (only used by the DPP-RL extension) ??????
# stable-baselines3>=2.3
# gymnasium>=0.29

# ?? Optional: real post-quantum benchmarks (impl/pqc/timing.py) ?????
# liboqs-python is non-trivial to build on Windows. The notebook falls
# back to a deterministic stub model if it cannot be imported.
# liboqs-python>=0.10
